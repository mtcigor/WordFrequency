/**
 * @igormtc
 * July 2024
 * Main function
 */

#include <stdio.h>
#include <malloc.h>
#include <stdbool.h>

#include "WordsFrequency.h"
#include "HashStuff.h"

int main (){
    bool* error = (bool*)malloc(sizeof(bool));
    FileCounted* file = InitializeCounter(10);
    return 0;
}